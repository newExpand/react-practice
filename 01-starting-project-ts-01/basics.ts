let age: number = 24;

age = 12;

let userName: string;

userName = "민수";

let isInstructor: boolean;

isInstructor = true;

// 배열에 타입주기

let hobbies: string[];

hobbies = ["축구", "농구"];

// 객체에 타입주기

let person: {
    name: string;
    age: number;
};

person = {
    name: "민수",
    age: 32,
};

// person = {
//     isEmployee: true
// }

// 배열 안의 객체에 타입주기

let people: {
    name: string;
    age: number;
}[];
